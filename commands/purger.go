package commands

import (
	"github.com/co0p/neo4ipool/neo4j"
)

type Purger struct {
	GraphDB neo4j.GraphDB
}

func (p Purger) Run() (string, error) {

	// parse json

	// create nodes

	// create references

	// push into db

	return "", nil
}
