package commands

import (
	"github.com/co0p/neo4ipool/graphdb"
)

type TopicDetect struct {
	Filepath string
	GraphDB  *graphdb.GraphDB
}

func (i TopicDetect) Run() error {

	// parse json

	// create nodes

	// create references

	// push into db

	return nil
}
